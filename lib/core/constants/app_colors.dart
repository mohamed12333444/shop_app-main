import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary Palette - Saudi Green
  static const Color primary = Color(0xFF006C35);
  static const Color primaryLight = Color(0xFF339966);
  static const Color primaryDark = Color(0xFF004D26);

  // Accent
  static const Color accent = Color(0xFF006C35); // Same as primary for consistency
  static const Color accentLight = Color(0xFF339966);

  // Background - White
  static const Color background = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFF5F5F5);
  static const Color cardBg = Color(0xFFFFFFFF);
  static const Color divider = Color(0xFFE0E0E0);

  // Text
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF666666);
  static const Color textMuted = Color(0xFF999999);

  // Status
  static const Color success = Color(0xFF006C35);
  static const Color warning = Color(0xFFFFBB0D);
  static const Color error = Color(0xFFFF4D6A);

  // Shimmer
  static const Color shimmerBase = Color(0xFFE0E0E0);
  static const Color shimmerHighlight = Color(0xFFF5F5F5);

  // Star rating
  static const Color star = Color(0xFFFFBB0D);
}
